services:
  web:
    image: node:22-slim
    working_dir: /app
    volumes:
      - ./:/app
      - web_node_modules:/app/node_modules
    ports:
      - "3000:5173"
    env_file:
      - ./.env.base44-defaults
      - /run/base44/app.env
    command: sh -c "corepack enable pnpm && pnpm install --frozen-lockfile && pnpm dev"
    restart: unless-stopped

volumes:
  web_node_modules:
